<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003" > 
	<ItemGroup>
		<JsonLoaderSolution Include="$(SourceRoot)\..\example\JsonLoader\JsonLoader.sln" />

		<Solutions Include="@(JsonLoaderSolution)" />
	</ItemGroup>
  
	<Target Name="BuildMethod">
		<MSBuild Projects="@(JsonLoaderSolution)" Properties="@(Properties)" />
	</Target>
</Project>

