<Project DefaultTargets="Build" xmlns="http://schemas.microsoft.com/developer/msbuild/2003" > 
	<ItemGroup>
		<ServiceSolution Include="$(SourceRoot)\kCura.IntegrationPoints.Services.sln" />

		<Solutions Include="@(ServiceSolution)" />
	</ItemGroup>
  
	<Target Name="BuildMethod">
		<MSBuild Projects="@(ServiceSolution)" Properties="@(Properties)" />
	</Target>
</Project>

