
*** Settings ***
| Documentation 	| An init file used to prepare a Legal Hold RDO Job. 
| ...				| It opens a browser window, creates the Automated RDO Checking Layout (unless it already exists), 
| ...				| goes to the Fields tab, then selects that Layout, and changes the item per page count.
| ...				| This prepares the Job to run any/all of the RDO suites.
| Suite Setup		| Open the Browsers and then Go to the Fields tab and use the Automated RDO Checking View
| Suite Teardown	| Close All Browsers
| Force Tags		| feature.legal_hold	| team.game_of_holds


*** Variables ***

| ${WorkspaceName}	| LDAP Sync


*** Keywords ***

| Open the Browsers and then Go to the Fields tab and use the Automated RDO Checking View
| | Setup connection to the relativity database
| | Open and Log in to Legal Hold
| | Go to the Tab	| Fields
| | @{FieldValueList}=	| Create List	| Name	| Automated RDO Checking	| Order	| 99999	| Object Type	| Field
| | @{FieldList}=	| Create List	| Object Type	| Name	| Field Type	| Is Required	| Linked	| Associative Object Type
| | @{EmptyList}=	| Create List	| 
| | @{SortList}=	| Create List	| Object Type	| Ascending	| Name	| Ascending
| | ${STATUS}	| ${MESSAGE}=	| Run Keyword And Ignore Error	| Verify the View is available	| Automated RDO Checking
| | Run Keyword If	| '${STATUS}' == 'FAIL'	| Create View	| ${FieldValueList}	| ${FieldList}	| ${EmptyList}	| ${SortList}
| | Run Keyword If	| '${STATUS}' == 'FAIL'	| Go to the Tab	| Fields
| | Wait Until Keyword Succeeds	| 10s	| 2s	| Set View	| Automated RDO Checking
| | Select From List	| xpath=//select[@name='ctl00$ctl32']	| 100	| # Change the Items per page count
| | Wait Until Page Element Reloads	| id=ctl00_ctl00_itemList_itemListHead	| # The Item List
| | [Teardown]	| Drop connection to the relativity services api


*** Settings ***
| Library	| ${KEYWORD_DIR}/kWebDriver.py
| Resource	| ${KEYWORD_DIR}/Relativity/Shared/Views/ViewKeywords.txt
| Resource	| ${LEGAL_HOLD_KEYWORD_DIR}/BackendKeywords.txt
| Resource	| ${LEGAL_HOLD_KEYWORD_DIR}/NavigationKeywords.txt