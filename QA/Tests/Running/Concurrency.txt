
*** Settings ***
| Documentation 	| Verify that the Next and Back buttons on the Integration Point Wizard have the correct text on each Step.
| Metadata		    | Authors	| William Sehy
| Force Tags		| in-progress	| team.method	| manual
#| Suite Setup		| Log	| This is the suite setup keyword.
#| Test Setup		| Open and Log in to Method
#| Test Teardown 	| Close All Browsers
#| Suite Teardown	| Log	| This is the suite teardown keyword.


*** Variables ***

| ${WorkspaceName}	| Relativity Legal Hold
| ${IntegrationPoint1}	| 
| ${IntegrationPoint2}	| 
| ${IntegrationPoint3}	| 
| ${IntegrationPoint4}	| 
| ${Source1}			| 
| ${Source2}			| 


*** Test Cases ***

| Importing the same Object Type from two different Sources
| | [Tags]			| manual	| integration_points
| | Run the following manual steps:
| | ... | Preconditions:
| | ... | 1. ${IntegrationPoint1} uses ${Source1} to import Custodians
| | ... | 2. ${IntegrationPoint1} has [X] Custodians to import
| | ... | 3. ${IntegrationPoint2} uses ${Source2} to import Custodians
| | ... | 4. ${IntegrationPoint2} has [Y] Custodians to import
| | ... | 5. [X] and [Y] are both at least one
| | ... | 
| | ... | Steps:
| | ... | 1. Set both ${IntegrationPoint1} and ${IntegrationPoint2} to import five minutes from now
| | ... | 2. Wait until the Jobs finish
| | ... | 
| | ... | Expected Results:
| | ... | 1. Verify that the ${IntegrationPoint1} imported all [X] Custodians correctly
| | ... | 2. Verify that the ${IntegrationPoint2} imported all [Y] Custodians correctly


| Importing different Object Types from the same Source
| | [Tags]			| manual	| integration_points
| | Run the following manual steps:
| | ... | Preconditions:
| | ... | 1. ${IntegrationPoint1} uses ${Source1} to import Custodians
| | ... | 2. ${IntegrationPoint1} has [X] Custodians to import
| | ... | 3. ${IntegrationPoint3} uses ${Source1} to import Projects
| | ... | 4. ${IntegrationPoint3} has [Y] Projects to import
| | ... | 5. [X] and [Y] are both at least one
| | ... | 
| | ... | Steps:
| | ... | 1. Set both ${IntegrationPoint1} and ${IntegrationPoint3} to import five minutes from now
| | ... | 2. Wait until the Jobs finish
| | ... | 
| | ... | Expected Results:
| | ... | 1. Verify that the ${IntegrationPoint1} imported all [X] Custodians correctly
| | ... | 2. Verify that the ${IntegrationPoint3} imported all [Y] Projects correctly


| Importing the same Object Type from the same Source
| | [Tags]			| manual	| integration_points
| | Run the following manual steps:
| | ... | Preconditions:
| | ... | 1. ${IntegrationPoint1} uses ${Source1} to import Custodians
| | ... | 2. ${IntegrationPoint1} has [X] Custodians to import
| | ... | 3. ${IntegrationPoint4} uses ${Source1} to import Custodians
| | ... | 4. ${IntegrationPoint4} has [Y] Custodians to import
| | ... | 5. [X] and [Y] are both at least one
| | ... | 
| | ... | Steps:
| | ... | 1. Set both ${IntegrationPoint1} and ${IntegrationPoint4} to import five minutes from now
| | ... | 2. Wait until the Jobs finish
| | ... | 
| | ... | Expected Results:
| | ... | 1. Verify that the ${IntegrationPoint1} imported all [X] Custodians correctly
| | ... | 2. Verify that the ${IntegrationPoint4} imported all [Y] Custodians correctly


| Importing different Object Types from different Sources
| | [Tags]			| manual	| integration_points
| | Run the following manual steps:
| | ... | Preconditions:
| | ... | 1. ${IntegrationPoint2} uses ${Source2} to import Custodians
| | ... | 2. ${IntegrationPoint2} has [X] Custodians to import
| | ... | 3. ${IntegrationPoint3} uses ${Source1} to import Projects
| | ... | 4. ${IntegrationPoint3} has [Y] Projects to import
| | ... | 5. [X] and [Y] are both at least one
| | ... | 
| | ... | Steps:
| | ... | 1. Set both ${IntegrationPoint2} and ${IntegrationPoint3} to import five minutes from now
| | ... | 2. Wait until the Jobs finish
| | ... | 
| | ... | Expected Results:
| | ... | 1. Verify that the ${IntegrationPoint2} imported all [X] Custodians correctly
| | ... | 2. Verify that the ${IntegrationPoint3} imported all [Y] Projects correctly


*** Settings ***
| Library	| ${KEYWORD_DIR}/ManualKeywords.py
| Library	| ${KEYWORD_DIR}/kWebDriver.py
| Resource	| ${METHOD_KEYWORD_DIR}/NavigationKeywords.txt